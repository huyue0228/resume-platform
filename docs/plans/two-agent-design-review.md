# 双 Agent 设计检查记录

日期：2026-09-14。检查对象：[项目设计](2026-09-14-two-agent-design.md)。本文件记录设计演算、源码对照与图形验证，不是生产功能验收报告。

## 1. 业务约束追溯

| 已确认要求 | 设计落点 | 对应验收 |
|---|---|---|
| B01/B02：先筛选，且仅当前有效投递 | 第 3、4、8 节；筛选 v3 原语义保持 | A01、A09、A12 |
| B03：达标进入内部池 | 第 4、7、9 节；资格版本与分配工作项同事务建立 | A01、A18、A20 |
| B04：分配不分析原始简历 | 第 7、8 节；严格投影、独立工具白名单 | A08、A10、A19 |
| B05/B08：按既定优先级，同级才均衡 | 第 6 节；机器可复算排序向量 | A04–A09、A14 |
| B06：非公开内部岗位能接收 | 第 5、6 节；显式映射与接收状态 | A02、A09 |
| B07：归属 HC 不消耗招聘人数 | 第 5、12 节；新路径不写容量 reservation | A03、A17 |
| B09/B10：平台执行且唯一当前去向 | 第 9 节；范围版本、fencing、数据库唯一约束 | A11–A15、A20 |

复核后的边界：内部映射可以触达已配置的非公开需求，但不能扩大筛选 Agent 的当前投递标准；分配 Agent 不继承筛选工具或全局 MCP Provider；正式录用、自动同池转荐、额外模型编排均为后续范围。

## 2. 设计演算

运行：

```sh
python3 docs/plans/verify-two-agent-design.py
```

结果：16 个手工预期的合成案例通过；112 次 HC 数值、公开状态及传输顺序变化检查通过，无失败。所有输入均为合成标签与整数引用，没有真实简历或密钥。

这些检查只证明设计中的纯规则演算与预期一致。引用字段的生产 Schema、标签证据来源真实性、真实数据库并发、权限、跨进程恢复、模型质量和业务收益均未由此验证。主设计 A01–A20 明确列出实施验收内容。

## 3. 流程图验证

图形类型：workflow。图源和独立 HTML 均在 `docs/architecture/`。

```text
diagram_type: workflow
output: docs/architecture/two-agent-screening-allocation.html
specification_sha256: cb0149fdd1f125853ae7456c940e25f089741a4cff7bd531ffbf96e519fcba00
specification_bytes: 5131
artifact_sha256: de1bca7e1ca38f8be2965dcee6bb209c0a21c288c8dca3eac87860e912c202a9
artifact_bytes: 714096
validation: 9/9 showcase, 0 errors, 0 warnings
browser_evidence: passed
visual_review: passed
correction_rounds: 2
```

设计阶段的自动浏览器检查绑定上述 HTML 摘要；临时回执与截图已在 v3.1.0 发布清理中移除，保留正式图和 workflow 源文件。Safari 实测 1440×900、1600×1000、1920×1080、2048×1320 四种视口，均无横向或纵向溢出，文字可读性与工具栏布局检查通过。初次沙箱端口监听失败后，在允许的本机浏览器自动化环境重新完成检查；最终状态取最新同摘要检查结果。

图像复核查看了 1440×900、2048×1320 的深浅主题四张实际截图：主路径、等待分支、返回路径、标签和边界卡片均可辨认，未发现跨节点连线、文字遮挡或内容裁切。图像复核是本助手对截图的观察，不是用户人工签收。未额外验收搜索、导出、演示和交互面板的所有功能；图形工具自身的这些能力不属于本次业务设计验收。

以上为设计阶段的检查记录，不代表本次发布对全部页面交互或生产环境的验收。

## 4. 实施前必须落实的工程条件

- 分配统计按稳定需求归属计算；历史无法证明归属的记录必须在迁移报告中单列。初期存在统计不完整时须展示覆盖范围，不能宣称已经实现历史供给的精确均衡。
- 当前文件存储若允许原位替换，先落实源文件不可变版本及失效机制，再移除分配时文件摘要读取。
- 所有创建、取消、改派、反馈、需求配置入口必须统一范围锁序与 revision 更新，不能只修改自动分配路径。
- 新协议、Schema 和模拟服务从 contracts 源模型生成；两消费者继续独立构建。
- 近 7 天窗口、100 人/200 需求批次上限、60 秒快照有效期是本设计提出的初始默认值，后续通过数据与负载验证调整，不应表述为用户指定数值。

## 5. 交付范围

本次新增主设计、合成案例、设计参考计算器、目标流程图及检查证据。没有修改运行代码、数据库、生成协议、现有四份设计文档或既有未提交修改；没有构建/发布镜像或执行 stage、commit、push。

设计已经具备进入 P1 协议实现的基础；实际功能完成仍需执行主设计中的 P1–P6 工作项及对应验收。
